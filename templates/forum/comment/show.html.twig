{% extends 'base.html.twig' %}

{% block title %}SlackOverflow{% endblock %}

{% block body %}
	{# <h3>
		{{ topic.title }} is topic {{ topic.id }}
	</h3>
	<p>
		{{ topic.content }}
	</p> #}
	<p>
		{{ comment.author }} answered<br>
		{{ comment.content}} the {{ comment.createdAt | date("m/d/Y")}} at {{ comment.createdAt | date("H:i:s")}}
	</p>
{% endblock %}
